function solve (num){
    // if(num==0) return 0; 
    // let maxdigit=0 , ans=0, num1=num;
    // while(num1){
    // maxdigit = Math.max(num1%10, maxdigit)
    // num1/=10;
    // num1= Math.floor(num1);
    // }
    // // console.log(maxdigit)

    // return   1 + solve (num - maxdigit);
}
console.log(solve(37882))